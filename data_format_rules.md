# Deep Past Challenge - Data Format Rules Summary
基于官方更新帖 "A Stitch in Time Saves Nine" 以及评论区补充信息，全面总结的模型训练数据对齐要求。

> [!IMPORTANT]
> 主办方的核心诉求是：过去的标注数据极度复杂且不统一。为了模型能正确在隐藏测试集上得分，**必须将训练集的格式和测试集严格对齐**。如果不做对齐，即使模型翻译对了，也会因为格式问题（如标点、单位、gap标记不同）被扣分。

---

## 1. 强制对齐项 (必须执行)
这些更改是主办方已经在最终评估用的 **Test Data** 中应用了的。如果不照做，预测结果必定会因为格式不匹配而大幅掉分。

### 1.1 Gap (残缺) 标记大一统
所有类型的损坏、缺失标记，以及连续的多个标记，**全部合并为唯一的一个 `<gap>` 标签**。
* **替换目标**：
  * 单个占用符：`x`, `[x]`
  * 省略号：`…` (包含各种长短的省略号)
  * 文字标注：`(break)`, `(large break)`, `(n broken lines)`
* **去重逻辑**：
  * **消除 `<big_gap>`**：所有的 `<big_gap>` 现已废弃，全部转为 `<gap>`。
  * **消除连续 Gap**：类似 `<gap> <gap>`, `-<gap> <gap>`, `<big_gap> <big_gap>-` 等一律折叠缩减为单个 `<gap>`。
  * **最终结果**：整句话中不应该存在任何 `<big_gap>`，也不应该存在连续的两个 `<gap> <gap>`。

### 1.2 Determinatives（限定词）括号替换
测试集中的特定限定词格式已从圆括号统一改为了花括号或无括号：
* `(d)` ➔ `{d}`
* `(ki)` ➔ `{ki}`
* `(TÚG)` ➔ `TÚG`

### 1.3 浮点数截断
过长的浮点数必须被**截断（保留到小数点后第 4 位）**，而不是四舍五入或保留极长精度：
* `1.3333300000000001` ➔ `1.3333`
* `2.6666600000000003` ➔ `2.6666`

---

## 2. Translations (目标英文) 处理推荐
这是主办方强烈推荐的训练集清理步骤，用于去除原本数据集中给人类学者看的“杂音”。

### 2.1 需移除的杂音 (Remove)
* **语法标签**：`fem.`, `sing.`, `pl.`, `plural`
* **不确定性标记**：`(?)`
* **无意义的零碎符号 (Stray marks)**：`..`, 无意义的 `?`, 孤立的 `x` 或 `xx`, `<< >>`, `< >` (特别注意：包裹 gap 的 `< >` 不能删)。
* **多选翻译**：包含 `/` 表示两种可选翻译的（例："you / she brought"），必须截断**只保留其中一个**（例："you brought"）。

### 2.2 必须保留的符号 (Do NOT Remove)
这些符号在测试集中依然存在，切勿误杀：
* 双引号：`" "`
* 单引号/撇号：`'`
* 有实际语意/感情色彩的：`?` 和 `!`

### 2.3 特定词汇与单位的定式替换 (Replace)
* **人名佔位符**：`PN` ➔ `<gap>`
* **特定物品加前缀**：
  * `-gold` ➔ `pašallum gold`
  * `-tax` ➔ `šadduātum tax`
  * `textiles` ➔ `kutānum textiles` (注：评论区指出如果有连字符如 `kutānu-textiles` 或 `14 textiles import-tax`，应对应处理掉连字符或正确替换)。
* **重量单位精确转换** (Shekel 转 Grains)：
  * `1 / 12 (shekel)` ➔ `⅔ shekel 15 grains`
  * `5 / 12 shekel` ➔ `15 grains`
  * `5 11 / 12 shekels` ➔ `6 shekels less 15 grains`
  * `7 / 12 shekel` ➔ `½ shekel 15 grains`

### 2.4 月份罗马数字转阿拉伯数字
* Month I, Month II, Month III ... ➔ Month 1, Month 2, Month 3 ... 
* 最高到 Month XII ➔ Month 12。

---

## 3. Transliterations (输入阿卡德语) 处理推荐

### 3.1 字符标准化
* `Ḫ` ➔ `H`
* `ḫ` ➔ `h`
* `KÙ.B.` ➔ `KÙ.BABBAR`

### 3.2 下标数字转常规数字
Unicode 的下脚标数字必须转为同等的阿拉伯整数：
* `₀` ➔ `0`,  `₁` ➔ `1`,  `₂` ➔ `2` ... 直到 `₉` ➔ `9`。

---

## 4. 通用：小数转 Unicode 分数符号
无论是 Translation 还是 Transliteration，指定的小数应直接转换为特殊的 Unicode 分数符（注意这不可与长浮点截断规则冲突，建议对纯粹的固定小数模式执行此替换）：
* `0.5` ➔ `½`
* `0.25` ➔ `¼`
* `0.75` ➔ `¾`
* `0.3333` ➔ `⅓`
* `0.6666` ➔ `⅔`
* `0.1666` ➔ `⅙`
* `0.8333` ➔ `⅚`
* `0.625` ➔ `⅝`

---

## 5. 评论区发现的数据问题风险提示
> [!WARNING]
> 选手在应用新版数据时需要注意避坑的官方失误：
1. **Transliteration 数据截断灾难**：最新的一版训练数据（V3）中，有大约 10% 的长片语被异常截断丢失了数据（例如长度从 592 个字符被腰斩到了 138 个）。选手若要追求极致，可能需要通过 `published_texts.csv` 的 `OARE_Text_ID` 去 PDF 里自己找回或者合并上一版数据。
2. **LB 分数未实时更新**：官方人员证实目前排行榜(Leaderboard)分数更新出了点 Bug，可能会导致显示的最好分数与实际不符，不必过分纠结。
